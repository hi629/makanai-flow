{
  "name": "makanai-flow-ai-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "login": "wrangler login",
    "dev": "wrangler dev --env development",
    "deploy:dev": "wrangler deploy --env development",
    "deploy:prod": "wrangler deploy --env production",
    "secret:dev": "wrangler secret put GEMINI_API_KEY --env development",
    "secret:prod": "wrangler secret put GEMINI_API_KEY --env production"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.56.0"
  }
}
